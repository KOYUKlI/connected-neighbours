spring:
  datasource:
    url: jdbc:h2:file:./data/db
    username: sa
    password: ""
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
  h2:
    console:
      enabled: true
      path: /h2-console

api:
  base-url: ${API_BASE_URL:http://localhost:3000/api}
